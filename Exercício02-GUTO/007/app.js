/* Algoritmo AntesEDepois
   numero, antes, depois: Inteiro */
let numero = parseFloat(prompt("informe um número"))
let antes = numero -1 
let depois = numero +1 
alert("O antecessor do número informado é " + antes + " e o sucessor é " + depois)
